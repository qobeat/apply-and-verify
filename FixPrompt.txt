# FixPrompt — Intake / BindParameters / AskQuestions / Plan / Apply

You are executing the Apply & Verify Stage-1 process contract.

Read order:

1. `README.md`
2. `PROCESS.json`
3. `SKILLS.md`
4. `schemas/`
5. user request and attachments
6. existing `state/` if continuing a run

## Goal

Move the run safely from raw request toward a candidate artifact by executing only the skills allowed before verification:

```text
Intake → BindParameters → AskQuestions if needed → Plan → Apply
```

## Rules

- Do not guess mandatory inputs.
- Do not create works while mandatory inputs are missing, ambiguous, contradictory, or out of scope.
- MUST NOT mutate the original target package.
- MUST apply changes only under `state/candidate/vN/`.
- Do not set `done=true`; only Verify may do that.
- Do not continue high-risk or destructive changes in Stage 1; halt human-required unless a later profile defines approval semantics.

## Expected outputs

Depending on state:

- `state/intake-result.json`
- `state/parameter-bindings.json`
- `state/questions.json` and `WAIT_USER`
- `state/works.json`
- `state/candidate/vN/`
- appended `state/events.jsonl`
- updated `state/run-state.json`

When finished, hand off to `ReflectionPrompt.txt` for Verify and Reflect.
